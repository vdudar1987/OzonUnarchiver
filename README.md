# 🗂️ Ozon Unarchiver

Утилита для автоматического восстановления товаров из архива маркетплейса **Ozon**.  
Программа анализирует товары через API и восстанавливает только те, которые были отправлены в архив вручную.

---

## 🚀 Возможности

- 📊 Загрузка **offer_id** из Excel-файлов  
- 🔍 Проверка статуса товаров через **Ozon API**  
- ♻️ Восстановление **только ручного архива** (игнорирует автоархив)  
- 📈 Генерация детального отчета в **Excel**  
- 👥 Поддержка **нескольких аккаунтов Ozon**  
- 🖥️ Графический интерфейс на **tkinter**  
- ⚡ Многопоточная обработка  

---

## ⚙️ Установка

### Вариант 1: Запуск через EXE (рекомендуется для Windows)
1. Скачайте последнюю версию v1.1 из [Releases](../../releases).  
2. Распакуйте архив в любую папку.  
3. Запустите `main.exe` из папки `dist`.  

### Вариант 2: Запуск из исходного кода
Клонируйте репозиторий:
```bash
git clone https://github.com/yourusername/ozon-unarchiver.git
cd ozon-unarchiver
```
Запустите программу:
```bash
python main.py
```
## 🏗️ Сборка EXE
```bash
pip install pyinstaller
pyinstaller --onefile --noconsole --add-data "config.json;." --name main.exe main.py
```
Собранный файл будет находиться в папке `dist/main.exe`.

## 🔧 Настройка

Создайте файл config.json с настройками аккаунтов:
```json
{
  "accounts": [
    {
      "name": "Мой магазин",
      "client_id": "your_client_id",
      "api_key": "your_api_key"
    }
  ]
}
```

## 📖 Использование

Запустите программу (`main.exe` или `python main.py`).

Выберите аккаунт из списка.

Загрузите `Excel-файл` с колонкой `offer_id`.

Нажмите "Запустить".

Получите отчет `result.xlsx` с результатами.

## 📂 Формат входного файла

`Excel-файл` должен содержать колонку `offer_id` с идентификаторами товаров.

## 📊 Формат отчета

Программа генерирует отчет с колонками:

`offer_id` – идентификатор товара

`product_id` – внутренний ID Ozon

`status_before` – статус до обработки

`archived` – находится ли в архиве

`is_autoarchived` – автоархив или ручной

`action` – выполненное действие

`error` – ошибки (если есть)

## 🖥️ Требования

Для EXE версии: Windows 7+

Для Python версии: `Python 3.8+`, `requests`, `openpyxl`, `tkinter`

## 🆘 Поддержка

Если у вас возникли проблемы:

Проверьте правильность API-ключей в `config.json`

Убедитесь, что `config.json` находится в одной папке с программой

Проверьте формат `Excel-файла`

## 📜 Лицензия

Проект распространяется под лицензией MIT License.


## Сказать спасибо
**USDT TRC-20: THpKP86GX6L7BVQZU3Fk1JAvSKYyAt9cBf**

**USDT ERC-20:  0x80CdF230DEBF92522d318F7E1B32D22cA537e932**

**BTC:  3NiBDirGhQVJfnJ7bnewahA7YsZDD3EHvq**

**PAYEER:  P1072273155**

**YOOMONEY: 4100 1180 2998 2608**
